{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "start": "node .output/server/index.mjs",
    "build:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxi build",
    "generate:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxi generate"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nuxt3": "latest",
    "sass": "^1.50.0"
  }
}
