# ⚠️ 站外引用：請勿刪除

這個目錄的檔案是**給站外使用**的公開資料端點，
本站的原始碼裡不會有任何引用——用「搜尋引用次數」的方式判斷會誤判成孤兒檔而刪掉。

| 檔案 | 用途 |
| --- | --- |
| `TOWN_MOI_1100415.json` | Neil 在 CodePen 上的作品以此網址取用資料 |

網址一旦公開就等於承諾，改名或刪除會直接讓站外的東西壞掉。
要異動前先確認 CodePen 那邊已經改好。
